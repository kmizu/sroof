// Uses stdlib/Vec.sroof declarations directly (prepend stdlib source before this file)

defspec concat_nil_use(A: Type, m: Nat, ys: Vec(A)(m)): concat(A, Nat.zero, m, Vec.nil, ys) = ys {
  by trivial
}
